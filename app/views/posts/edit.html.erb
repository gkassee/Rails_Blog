<%= form_for @post do |f| %>
	<%= f.hidden_field :user_id, :value => @user.id%>
	<%= f.label :Title %>
	<%= f.text_field :title %>
	<br>
	<%= f.label :Body %>
	<%= f.text_area :body %>
	<br>
	<%= f.submit "Save Changes" %>
<% end %>